
<% if @submenubutton.subpages.size != 0 %>
<div id="sublinks">
	<%= link_to @submenubutton.title, @submenubutton, :class => 'at'%>
</div>
	<% @submenubutton.subpages.each do |s| %>
	<div id="sublinks">
		<%= link_to s.title, s, :class => 'other'%>
			</div>
	<% end %>

<% end %>

<% if @submenubutton.title == "Team Ullern"%>
<div id="sublinks">
	<%= link_to "Spillere", players_path, :class => 'other' %>
</div>
<% end %>
<div id="content">
<h1><%= @submenubutton.title %></h1>
 <%= raw  @submenubutton.content %>
</div>




